import React, { FC, FocusEvent } from "react";

type Props = {
  onCreate: { text: string; link: string };
};

const Form: FC<Props> = ({ onCreate }) => {
    return (
        <>
        </>
    )
};

export default Form;
